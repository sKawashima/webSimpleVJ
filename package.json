{
  "name": "SimpleVJ",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "electron-prebuilt": "^1.4.13",
    "stylus": "^0.54.5"
  },
  "scripts": {
    "start": "yarn front-build; electron index.js",
    "dev": "parcel front/src/*.pug --open",
    "front-build": "parcel build front/src/*.pug --target electron --out-dir front/"
  },
  "devDependencies": {
    "parcel-bundler": "^1.11.0",
    "pug": "^2.0.3"
  }
}
